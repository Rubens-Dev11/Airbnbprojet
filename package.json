{
  "name": "plateforme-douala-monorepo",
  "private": true,
  "description": "Monorepo — plateforme de réservation de chambres meublées à Douala. Nom de travail : le nom commercial n'est pas arrêté (voir docs/memory/decisions/ADR-003).",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "db:test": "bash supabase/tests/run.sh",
    "check": "pnpm typecheck && pnpm --filter @app/ui-tokens check:contrast && pnpm db:test",
    "db:types": "bash supabase/gen-types.sh",
    "tokens:css": "node --experimental-strip-types packages/ui-tokens/scripts/emit-css.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "supabase": "^2.115.0",
    "typescript": "^7.0.2"
  }
}
